<?php

return [

    /*
    |--------------------------------------------------------------------------
    | ค้นหาสถานประกอบการจาก OpenStreetMap
    |--------------------------------------------------------------------------
    |
    | ใช้เมื่อชื่อที่กรอกไม่มีในฐานข้อมูลของระบบ — ครอบคลุมร้านค้า โรงงาน และ
    | สำนักงานที่ไม่ได้จดเป็นนิติบุคคล หรือจดก่อนปี 2565 ซึ่งไม่มีในชุดข้อมูล
    | ของ DBD
    |
    | ทำงานเมื่อผู้ใช้กดปุ่มค้นหาเท่านั้น ไม่ได้ยิงทุกครั้งที่พิมพ์ ตามนโยบาย
    | การใช้งานของ Nominatim ที่ห้ามใช้แบบ autocomplete และผลลัพธ์ถูก cache ไว้
    | เพื่อไม่ให้คำค้นเดิมถูกยิงซ้ำ
    |
    */

    'osm_lookup' => env('COMPANY_OSM_LOOKUP', true),

    'osm_endpoint' => env('COMPANY_OSM_ENDPOINT', 'https://nominatim.openstreetmap.org/search'),

    /*
    | Nominatim บังคับให้ระบุ User-Agent ที่ติดต่อกลับได้ ไม่งั้นจะถูกบล็อก
    | ตั้ง APP_URL ให้ถูกต้องก็พอ
    */
    'osm_user_agent' => env('COMPANY_OSM_USER_AGENT'),

    'osm_cache_days' => env('COMPANY_OSM_CACHE_DAYS', 30),

];
